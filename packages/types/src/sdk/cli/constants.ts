export enum CommandWord {
  BACKUPS = "backups",
  HOSTING = "hosting",
  ANALYTICS = "analytics",
  HELP = "help",
  PLUGIN = "plugins",
}

export enum InitType {
  QUICK = "quick",
  DIGITAL_OCEAN = "do",
}
